import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'next-price-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
